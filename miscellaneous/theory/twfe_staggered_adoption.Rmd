

```{r}
# Load packages
library(tidyverse)
library(here)
library(DIDmultiplegtDYN)
library(zeallot)
```

```{r}
# Reload Claude helper functions
source(here::here("miscellaneous/theory/cc_functions/twfe_staggered_adoption.R"))
```

## De Chaisemartin & d'Haultfoeuille (2024)

### Setup

#### Simulated data ("manual")

```{r}
# Simulation parameters
groups <- 5
time_periods <- 5
group_sizes <- c(100, 100, 100, 100, 100) # Unweighted estimators for now
treatments <- list(
                   c(0,1,0,0,1), # Group 1
                   c(0,0,0,0,0), # Group 2
                   c(0,0,0,0,1), # Group 3
                   c(0,0,1,0,0), # Group 4
                   c(0,1,1,1,1)) # Group 5
  
```













